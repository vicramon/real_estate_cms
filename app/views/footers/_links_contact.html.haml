%footer
  .content

    .right
      .logo
        = filepicker_image_tag site.logo_url
      .info
        = li_present site, 'phone'
        = li_present site, 'fax'
        = li_present site, 'email'
        %li= site.address
        %li= site.city_state_zip

    %nav
      - site.ordered_pages.each do |page|
        %li= link_to page.name, page
