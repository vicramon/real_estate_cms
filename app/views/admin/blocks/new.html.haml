%h1 Add a Block

= form_for block, url: admin_page_blocks_path(page) do |f|

  %dl
    %dt= f.label :page_id, "Page:"
    %dd= f.collection_select :page_id, site.pages.order(:name), :id, :name, { selected: page.id }

  = render 'admin/pages/block_form', f: f

  %dl
    %dt
    %dd= f.submit
