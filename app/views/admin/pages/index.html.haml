.create_new
  = link_to "Add New Page", [:new, :site]

%table
  %tr
    %th Name
    %th
    %th
  - pages.each do |page|
    %tr
      %td= page.name
      %td= link_to "edit", edit_admin_page_path(page)
      %td= link_to "delete", site, method: :delete, class: 'delete',
        data: { confirm: 'Are you sure you want to delete this site?' }
